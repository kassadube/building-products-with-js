import * as ActionTypes from '../actionTypes'

const initialState = {
  token: ''
};

export const auth = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};